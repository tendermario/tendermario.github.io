r.onreadystatechange = function () {
  var delimiter = document.getElementById('delimiter');
  var inputList = document.getElementById('input-list');
  var outputList = document.getElementById('output-list');
  var prefix = document.getElementById('output-list');

  // Goals:
  // 1. For a given input list, create an output list
  // 2. For an output list, modify it to have only the name and ID
  // 3. For a name and ID, add purple link, ID hex and padding
  // 4. For each purple link, add prefix and delimiter up to 255 characters separation
  // 5. For each purple link list, add button prefix
};